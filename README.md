# React + Tailwind + TypeScript + Vite + SurrealDB

A very simple TO-DO app with React and Typescript using vite as a building tool.
The database used was SurrealDB, configured to use IndexedDB as persistance.